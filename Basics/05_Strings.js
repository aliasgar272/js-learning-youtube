const name = 'ali'
const age = 50

// Template strings are the better option
console.log(`Hello my name is ${name.toUpperCase()} and my age is ${age}`);

const url = new String(`aliasgar.somaiya.edu is my e-mail`);

console.log(url.length);

// console.log(url.splice(18 , url.length));

console.log(url.split("-"))

